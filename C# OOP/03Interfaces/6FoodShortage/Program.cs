using System;
using System.Collections;

namespace FoodShortage
{
    class Program
    {
        static void Main(string[] args)
        {
            // Document doc = new Document("My title", "Some content");
            // doc.Title = "New Title";
            // IPrintable pDoc = doc;
            // pDoc.Print();
            // (doc as IPrintable).Print();
            // (pDoc as Document).Title = "Document title";
            // (doc as IPrintable).Print();
            // IEnumerable<string> myEnum = new System.Collections.IEnumerable<string>();
        }
    }
}